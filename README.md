# FastAPI Template

Шаблон для создания FastAPI сервисов с чистой архитектурой, PostgreSQL, Redis и Docker.

## 🚀 Особенности

- **Clean Architecture** - разделение на слои: presentation, application, infrastructure
- **Async/Await** - полностью асинхронный код
- **Dependency Injection** - контейнеры для управления зависимостями
- **PostgreSQL** - асинхронная работа с базой данных через SQLAlchemy 2.0
- **Redis** - кэширование и работа с очередями
- **Alembic** - миграции базы данных
- **Docker** - готовые контейнеры для разработки и продакшена
- **Middleware** - обработка исключений и логирование
- **Health Checks** - endpoints для проверки состояния сервиса

## 📁 Структура проекта

```tree
template-fastapi/
├── app/
│   ├── application/          # Бизнес-логика (сервисы)
│   │   ├── container.py      # Контейнер сервисов
│   │   ├── repositories/     # Интерфейсы репозиториев
│   │   └── services/         # Бизнес-сервисы
│   ├── infrastructure/       # Инфраструктура
│   │   ├── container.py      # Контейнер инфраструктуры
│   │   ├── db/
│   │   │   ├── postgres/     # PostgreSQL
│   │   │   └── redis/        # Redis
│   ├── presentation/         # API слой
│   │   ├── api/              # Роуты
│   │   ├── middleware/       # Middleware
│   │   └── schemas.py        # Pydantic схемы
│   ├── config.py             # Настройки приложения
│   ├── dependencies.py       # FastAPI dependencies
│   └── main.py               # Точка входа
├── scripts/                  # Скрипты запуска
├── tests/                    # Тесты
├── docker-compose.yaml       # Docker Compose конфигурация
├── dockerfile                # Dockerfile
├── alembic.ini               # Конфигурация Alembic
└── pyproject.toml            # Зависимости проекта
```
